# GEPA Buildout Onboarding

First command:

```bash
mix deps.get && mix ci
```

This repo is a single root Mix project. It demonstrates deterministic GEPA
domain tasks through the sibling `gepa_framework` path dependency.
